# Set MOCK_MODE=true to run the API with canned sample data — no database needed.
# Leave it false (or remove it) once you have SQL Server set up.
MOCK_MODE=true

# Real DB credentials (only needed when MOCK_MODE is false)
DB_SERVER=your-server.database.windows.net
DB_NAME=MIST_460_Group1
DB_USER=your-username
DB_PASSWORD=your-password
